export type TFeature = {
  icon: React.ReactNode;
  title: string;
  description: string;
};

export type TStat = {
  value: string;
  label: string;
};

export type TTrustPoint = {
  title: string;
  description: string;
};